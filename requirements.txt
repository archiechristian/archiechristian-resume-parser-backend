# Fixed version pins to resolve the 'proxy' argument error
supabase==2.10.0
httpx==0.27.2
gotrue==2.9.0

# Core backend requirements
flask==3.0.0
flask-cors==4.0.0
pdfplumber==0.11.0
spacy==3.7.2
python-dotenv==1.0.0

# Optional: Gunicorn for better production stability on Render
gunicorn==21.2.0